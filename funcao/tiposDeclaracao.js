console.log(soma(2, 4));

function soma(x, y) {
    return x + y;
}

const sub = function(x, y) {
    return x - y;
}

console.log(sub(2, 3));

const mult = function mult(x, y) {
    return x * y;
}

console.log(mult(2, 4));
